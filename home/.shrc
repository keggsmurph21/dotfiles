#
# shell-agnostic run commands
#

export EDITOR=vim
export VISUAL=vim
export WORKON_HOME=~/.virtualenvs
if [ -f "$HOME/.tokens" ]; then
    source "$HOME/.tokens"
fi

source /usr/bin/virtualenvwrapper_lazy.sh
source /usr/local/etc/aliases.sh
