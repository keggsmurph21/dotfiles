#!/bin/bash -e

sleeping=1

for pid in `pgrep xsleep`; do
	if [ $pid -ne $$ ]; then

		kill $pid
		sleeping=0

	fi
done

if [ $sleeping -eq 1 ]; then
	while true; do

		xset -display :0.0 dpms force off

	done
else

	xset -display :0.0 dpms force on

fi

